{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}Choose gangs{% endblock %}

{% block main %}
    <div class="form__container-EasyAdmin">
        <div class="form__container-input">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="form__container-btn">Next</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}